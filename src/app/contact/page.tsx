import React from 'react'
import { BannierContact, SectionForm } from '../../components'

const page = () => {
  return (
    <section className="flex flex-col  items-center justify-around h-full w-full">
      <BannierContact/>
       <SectionForm/>
    </section>
  )
}

export default page
